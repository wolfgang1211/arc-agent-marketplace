# Copy to ".env.local" and paste the contract address you deployed.
NEXT_PUBLIC_CONTRACT_ADDRESS=0xYourDeployedContractAddress
# Optional until the Envio deployment exists. Enables indexed job discovery
# and truthful current-era agent ranking on the dashboard.
NEXT_PUBLIC_ENVIO_GRAPHQL_URL=https://indexer.example.com/v1/graphql
